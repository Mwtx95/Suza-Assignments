#include <stdio.h>
#define size 100

struct stack 
{
int top
int age;
int name[size];
}stud;

void push(int item)
{
if (st.top >=size-1){
printf("stack overflow\n");
}
st.s[++st.top] = item;
}

int pop()
{
if ( st.top == -1)
{
printf("stack underflow\n");
return -1;
}
int item = st.s[st.top];
st.top--;
return item;
}
void  display()
{
for ( int i = 0; i<=st.top;i++)
printf("%d",st.s[i]);
}
